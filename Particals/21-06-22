Write a program to find whether a number is palindrome or not?
   Palindrome number in java: A palindrome number is a number that is same 
   after reverse. For example 545, 151, 34543, 343, 171, 48984 are the 
   palindrome numbers.
   
  public class PalindromeExample  {

	public static void main(String[] args) {
		{		
		int s = 121;
		int rem = 0;
		int sum = 0;
		int temp = s;
		while(s>0) {
			rem = s%10;
			sum = (sum * 10) + rem;
			s=s/10;
		}
		if(sum == temp) {
			System.out.println(sum + " it is palandrom");
		}
		else{
			System.out.println(sum +" not palandrom");
			}
		}

	}
	}
			  

Write a program to generate fibonacci series upto first 10 terms.
   In fibonacci series, next number is the sum of previous two numbers.
   
   public class FibonaciSeries {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		int n1 = 0 ,n2 = 1,n3 = 0;
		System.out.print(n1 + " " + n2);    
		for(int i  = 0 ; i <= 10 ; i++ ) {
			n3=n1+n2;    
			System.out.print(" "+n3);    
			n1=n2;    
			n2=n3;    
			
		}
		

	}

}
 
   
